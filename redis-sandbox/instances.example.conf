# One sandbox Redis instance per student: <port>:<password>
# `port` and `password` must match the student's `port` / `password` in the portal users
# collection (see `server/scripts/create-user.ts`).
#
# Copy this file to instances.conf (git-ignored) and restart the redis-sandbox service
# after editing it.
6380:change-me
