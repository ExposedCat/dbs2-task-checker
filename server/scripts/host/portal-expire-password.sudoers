# /etc/sudoers.d/portal-expire-password — lets the infrastructure user run the password-expiry
# helper (and nothing else) without a password, on behalf of the portal.
yuliia ALL=(root) NOPASSWD: /usr/local/libexec/portal-expire-password
